VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# CORS proxy Worker URL (worker/proxy-worker.ts). After `npx wrangler deploy`,
# paste the *.workers.dev URL here. Optional — public proxies are used if unset.
VITE_PROXY_URL=
